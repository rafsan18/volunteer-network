import React from "react";

const NoMatch = () => {
    return (
        <div>
            <h1 className="text-center">
                The path you selected does not exists
            </h1>
        </div>
    );
};

export default NoMatch;
